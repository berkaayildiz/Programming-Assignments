package enums;

// For priorities of tasks, we will use the following enum's ordinal values,
// therefore we must declare them in the order we want them to be prioritised.
public enum ManagementType
{
    FILE_MANAGEMENT,
    DEVICE_MANAGEMENT,
    USER_MANAGEMENT,
    MEMORY_MANAGEMENT,
    PROCESS_MANAGEMENT,
    SECURITY_MANAGEMENT,
}
